import React from 'react';
import { RadioButton } from 'rn-ui-kit';
import { RadioButtonProps } from '../../../lib/typescript/components/RadioButton';

export const MyRadioButton = (props: RadioButtonProps) => {
  return <RadioButton {...props} />;
};
