import React from 'react';
import { Switch } from 'rn-ui-kit';
import { SwitchProps } from 'react-native-switch';

export const MySwitch = (props: SwitchProps) => {
  return <Switch {...props} />;
};
